/** @type {import('next').NextConfig} */
const nextConfig = {
  // Mock-Daten brauchen keine externen Bilder; echte Cover (RAWG) werden über
  // ein einfaches <img>-Tag geladen und brauchen keine zusätzliche Bild-Config.
};

module.exports = nextConfig;
